# 茗韵时光 v3.2.2：修复 YouTube 直播声音，B站管理全新上线

茗韵时光 v3.2.2 正式发布。本次更新修复了长期困扰的 YouTube 直播无声问题，并将原「B站推流」升级为完整的「B站管理」专区——直播推流、个人收藏夹、视频批量下载、下载专区设置，一站搞定 B 站内容消费。

## 修复：YouTube 直播终于有声音了

此前 YouTube 直播一直「有画面没声音」，排查了很久终于锁定根因：

**yt-dlp 对直播流返回的音频格式缺少 `acodec` 字段**——音频流（如 format 233/234）只有 `resolution: "audio only"`，而旧代码用 `f.acodec && f.acodec !== 'none'` 筛选音频，把这些音频流全部误过滤掉了，导致 `liveAudioUrl` 恒为空。

修复方案：新增 `isAudioOnly` 判定——只要 `vcodec=none` 且 `resolution/format_note` 含 `audio` 就算音频流，不再依赖缺失的 acodec 字段。同时兼容 `rr*.googlevideo.com/videoplayback?...m3u8...` 形式的音频 URL（m3u8 藏在 query 里）。

顺带把直播延迟参数收紧（`liveSyncDurationCount` 5→2），播放头更贴近直播边缘（约 4~6 秒），看直播更实时。

## B站管理：从「推流」到「管理」

原来的「B站推流」Tab 升级为「B站管理」，包含两个子页：

### 直播推流

一键开播：选分区、填标题、选封面 → 生成 OBS 推流参数（服务器地址 + 串流密钥，一键复制）。这背后是一整套 B 站直播 API 排障成果：

- **分区保存失效**：`Room/update` 的分区参数名是 `area_id`（不是 `area_v2`，只有开播接口才用 `area_v2`），修正后改分区真正生效
- **封面上传 -617 文件过大**：直播封面专用图床目录限制极小（<566KB 即被拒）。改用 `api.bilibili.com/x/upload/web/image` 的 **`bucket=openplatform`**（限制 20MB），566KB 原图直传成功
- **标题/封面设置不生效**：`Room/startLive` 根本不接收 `title`/`cover` 参数，标题一直没设成功过。改为开播成功后统一走 `xlive/app-blink/v1/preLive/UpdatePreLiveInfo`（唯一接受 cover 的接口，cover 必须是 `.hdslb.com` 域名）

### 个人收藏夹

- 分页浏览（修正总数取错字段，1900+ 条收藏不再只显示 20 条）
- **番剧展开**：从收藏项的链接提取 ep 号 → 抓页面拿季号 → 列出全部集数，可批量下载
- **合集识别**：收藏夹里收藏「合集下的单条视频」时 B 站只返回普通视频信息，通过视频详情接口的 `ugc_season` 查出所属合集（如《八方旅人》142 集全部列出）
- 类型徽章（番剧/合集/视频）、自定义复选框、折叠动画
- 每项带「复制链接」按钮，一键复制分享链接

## 视频批量下载

收藏夹和网址解析结果都支持**勾选批量下载**：

- **严格串行**：先下完第一个再下第二个（事件驱动，30 分钟超时保护），避免同时下载拖垮网络
- **自动分文件夹**：每个视频存到 `下载区\视频标题\视频标题.mp4`，互不覆盖
- 设置页新增「**下载专区**」：自定义下载路径（默认系统下载区），之后所有下载不再弹窗

## 网址解析增强

- **粘贴即解析**：直接粘贴 B 站分享文案（如 `【超级马力欧银河大电影】 https://www.bilibili.com/...`），自动提取其中的链接，不用手动删文字
- **官方电影账号投稿**：`哔哩哔哩电影` 等官方账号投稿的电影，普通视频接口一律 404，已加 pgc 接口兜底（`pgc/player/web/playurl`），电影也能正常解析

## 其它调整

- 移除空间管理、稿件管理、UP投稿、上传稿件功能（收藏夹保留复制链接入口）
- B 站推流地址规范化，修复 OBS 一直重连的问题（串流密钥含前导 `?`，必须保留）

---

下载最新版体验：GitHub Releases → `茗韵时光 Setup 3.2.2.exe`
